<Window x:Class="WarehouseManagement.Views.AddInvoiceItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseManagement"
        mc:Ignorable="d"
        Title="Добавление позиции в накладную" 
        Height="300" Width="620" 
        WindowStartupLocation="CenterScreen">

    <Window.CommandBindings>
        <CommandBinding Command="local:WindowCommands.OrgOk" Executed="BtnAdd_Click"/>
        <CommandBinding Command="local:WindowCommands.OrgCancel" Executed="BtnCancel_Click"/>
    </Window.CommandBindings>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            
        </Grid.RowDefinitions>

        <ListBox x:Name="lbProducts" Grid.Row="0" 
         SelectionMode="Single"
         HorizontalContentAlignment="Stretch">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Article}" FontWeight="Medium" Margin="2 0"/>

                        <TextBlock Grid.Column="1" 
                           Text="{Binding Name}"/>

                        <TextBlock Grid.Column="2" 
                           Text="{Binding Price, StringFormat={}{0:N2} ₽}"/>

                        <TextBlock Grid.Column="3" 
                           Text="{Binding StockQuantity, StringFormat=Ост: {0}}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <GroupBox Header="Данные позиции" Grid.Row="1" Margin="0 12 0 0">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Количество:" Grid.Row="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtQuantity" Grid.Row="0" Grid.Column="1" Margin="8 0 0 0" Height="26"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
            <Button Content="Добавить в накладную" Width="160" Height="38" 
                    Command="local:WindowCommands.OrgOk" FontWeight="Bold" Margin="0 0 10 0"/>
            <Button Content="Отмена" Width="120" Height="38" 
                    Command="local:WindowCommands.OrgCancel"/>
        </StackPanel>
    </Grid>
</Window>