<Window x:Class="WarehouseManagement.Views.IncomingInvoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WarehouseManagement"
        mc:Ignorable="d"
        Title="Входящая накладная" Height="550" Width="850">

    <Window.CommandBindings>
        <CommandBinding Command="local:WindowCommands.OrgOk" Executed="BtnSave_Click"/>
        <CommandBinding Command="local:WindowCommands.OrgCancel" Executed="BtnCancel_Click"/>
    </Window.CommandBindings>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Входящая накладная" FontSize="18" FontWeight="Bold" Margin="0 0 0 12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 8">
            <Button Content="Добавить новый товар" Click="BtnAddItem_Click"/>
            <Button Content="Удалить позицию" Margin="8 0 0 0" Click="BtnRemoveItem_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgItems" Grid.Row="2" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул" Binding="{Binding Article}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ед." Binding="{Binding Unit}" Width="60"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 15 0 0">
            <Button Content="Сохранить" Width="160" Height="40" Command="local:WindowCommands.OrgOk" FontWeight="Bold"/>
            <Button Content="Отмена" Width="120" Height="40" Margin="10 0 0 0" Command="local:WindowCommands.OrgCancel"/>
        </StackPanel>
    </Grid>
</Window>