<Window x:Class="WarehouseManagement.Views.WhModWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local0="clr-namespace:WarehouseManagement"
        mc:Ignorable="d"
        Title="WhModWindow" Height="300" Width="450" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.CommandBindings>
        <CommandBinding Command="local0:WindowCommands.OrgOk" Executed="WhOk_Executed" />
        <CommandBinding Command="local0:WindowCommands.OrgCancel" Executed="WhCancel_Executed" />
    </Window.CommandBindings>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Наименование склада:" FontSize="14"/>
        <TextBox x:Name="tbWhName" Grid.Row="1" Margin="0,8,0,0" Height="28"/>
        <TextBlock Grid.Row="2" Text="Адрес:" FontSize="14" Margin="0,16,0,0"/>
        <TextBox x:Name="tbWhAddress" Grid.Row="3" Margin="0,8,0,0" Height="28"/>
        <TextBlock x:Name="tbError" Grid.Row="4" Margin="0,8,0,0" Foreground="DarkRed" FontSize="12"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Height="30" Command="local0:WindowCommands.OrgOk"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="90" Height="30" Margin="10,0,0,0" Command="local0:WindowCommands.OrgCancel"/>
        </StackPanel>
    </Grid>
</Window>

